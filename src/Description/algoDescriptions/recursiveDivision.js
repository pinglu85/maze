import { startNodeIcon, targetNodeIcon } from './sharedIcons';

const recursiveDivision = {
  icons: [startNodeIcon, targetNodeIcon],
  text:
    'Recursive Division is a fast algorithm. It builds walls, rather than breaking through them.',
};

export default recursiveDivision;
