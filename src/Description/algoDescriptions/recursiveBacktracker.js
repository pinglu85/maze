import huntAndKill from './huntAndKill';

const recursiveBacktracker = {
  ...huntAndKill,
  text:
    'Recursive Backtracker produces mazes that have long, twisty passages and relatively few dead ends. It is much faster than Hunt-and-Kill.',
};

export default recursiveBacktracker;
